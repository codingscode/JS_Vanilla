// isArray() - Array.isArray(obj)

const matriz = [2, 3, 8, 0, 1, -2]

const algo = {endereco: 'rua 7'}

const valor = 42

console.log(`${Array.isArray(matriz)} || ${Array.isArray(algo)}|| ${Array.isArray(valor)} `)
